/**
 * Navigation entry point (root navigator, linking, etc.).
 */

export { RootNavigator } from './RootNavigator';
export type {
  CreateTransactionScreenParams,
  RootStackParamList,
} from './types';
