/**
 * Application-wide configuration (env, API base URL, feature flags, etc.).
 */

/** Placeholder base URL for HTTP layer wiring; replace when a real API is chosen. */
export const API_BASE_URL = 'https://example.invalid/api';
